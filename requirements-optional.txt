# Optional extras. None of these are needed for the default pipeline.

# Alternative Whisper backend: the literal `openai-whisper` package, for
# reviewers who want the reference implementation. Enable with
# WHISPER_BACKEND=openai. Pulls in torch (~250 MB).
openai-whisper==20240930

# Alternative LLM providers, for higher extraction accuracy than a local 3B
# model. Enable with LLM_PROVIDER=anthropic (or openai) plus an API key.
langchain-anthropic==0.3.1
langchain-openai==0.2.14
