# Heavy / environment-specific dependencies, installed separately.
# Whisper (pick one backend):
faster-whisper>=1.0.0        # default backend (CTranslate2, fast on CPU)
# openai-whisper>=20231117   # reference backend; set WHISPER_BACKEND=whisper

# Test tooling
pytest>=8.0
httpx>=0.27                  # FastAPI TestClient
mongomock-motor>=0.0.29      # optional: exercise the Motor API in tests
pytest-asyncio>=0.23
