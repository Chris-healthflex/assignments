# ---- Web / API layer -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # multipart/form-data WAV upload

# ---- Validation ------------------------------------------------------------
pydantic==2.10.4                  # strict structured output (v2)
pydantic-settings==2.7.0

# ---- Transcription ---------------------------------------------------------
# Runs the OpenAI Whisper weights via CTranslate2: no torch, no ffmpeg,
# and ~4x faster on CPU. Whisper stays on the CPU so it never competes with
# the LLM for the 4 GB of VRAM.
faster-whisper==1.1.1
numpy>=1.26                       # used directly by app/transcription/audio_io.py

# ---- Extraction agent ------------------------------------------------------
langgraph==0.2.60
langchain-core==0.3.28
langchain-ollama==0.2.2           # default provider: local, no API key

# ---- Persistence -----------------------------------------------------------
motor==3.6.0                      # async MongoDB driver for FastAPI
pymongo==4.9.2

# ---- Tests -----------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1                     # FastAPI TestClient transport
mongomock-motor==0.0.34           # in-memory Mongo so CI needs no server
