# Fixtures

Place the assignment audio here:

    tests/fixtures/clinical_assessment.wav

Download it from the "Your Assignment" page (clinical_assessment.wav, ~8.9 MB).

The file is intentionally excluded from git by `.gitignore` — it is real
clinical audio and does not belong in a public repository.
