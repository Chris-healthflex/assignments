{
  "name": "first-assessment-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Review UI for the clinical first-assessment extraction service.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check-logic.mjs && node scripts/check-render.mjs",
    "test": "npm run check"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "vite": "^7.1.0"
  }
}
